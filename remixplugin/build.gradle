apply plugin: 'groovy'
apply plugin: 'maven'

dependencies {
    compile 'org.aspectj:aspectjtools:1.8.9'
    compile gradleApi()
    compile localGroovy()
}

repositories {
    mavenCentral()
}

group='com.tzduan.gradle'

version='1.0.0'

uploadArchives {
    repositories {
        mavenDeployer {
            repository(url: uri('/Users/tzduan/repo'))
        }
    }
}